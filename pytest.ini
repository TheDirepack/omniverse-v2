[pytest]
testpaths = backend/tests
pythonpath = backend
asyncio_mode = auto
# addopts = --cov=app --cov-config=setup.cfg --cov-report=term --cov-report=html
markers =
    slow: marks tests needing LLM or network (deselect with -m 'not slow')
    network: marks tests that make external network calls
