[pytest]
testpaths = tests_v2
pythonpath = .
addopts = -m "not network and not slow"
markers =
    unit: isolated unit test
    integration: database or API integration test
    ui: browser-facing test
    network: external network test
    slow: slow test
    evaluation: quality evaluation test
