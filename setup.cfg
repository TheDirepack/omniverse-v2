[tool:pytest]
# Mirrors pytest.ini — kept here for addopts only
addopts = --tb=short

[coverage:run]
source = backend/app
omit =
    */tests/*
    */__pycache__/*
    */main.py

[coverage:report]
fail_under = 55
show_missing = true
skip_covered = false
